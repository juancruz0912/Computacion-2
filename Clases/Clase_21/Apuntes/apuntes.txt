# ASYNC IO

no se sincroniza mi programa con los resultados que voy obteniendo

con await le digo al eventloop (pipeline, scheduler) que ya ocupe el tiempo de computo, que puede seguir con otra cosa. 
await devuelve un iterador, no el resultado de las cosas

yield from func(): se ejecuta la funcion que se llama hasta que haya un return o terminen todos los yield

gather(): otra forma de implementar evenloop, le doy una lista de awaitables y espera a que terminen. 
Paso mas de una corrutina
resultados = await asyncio.gather(mi_corrutina(), otra_corrutina()) o puedo hacer gather(*corrutinas), esto me devuelve un iterador, descomprime una lista

en las tareas de cpu bound, no tiene sentido usar asyncio.sleep, ya que usa mucho cpu

create_task: crea una tarea asíncrona para que el scheduler la ejecute
gather: espera a que todas las tareas terminen