# Imagen base liviana con python 3.12
FROM python:3.12-slim

WORKDIR /app
COPY server.py .

# Usuario no root y opcional (simple y seguro)
RUN useradd -m appuser
USER appuser

CMD ["python", "server.py"]
